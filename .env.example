# ==============================================================================
# FIXTURE APP - ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and .env.local and fill in your values
# NEVER commit .env or .env.local to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# API-FOOTBALL (REQUIRED)
# ------------------------------------------------------------------------------
# Get your API key from: https://www.api-football.com/
# Used for fetching live match data, standings, and competition information
# Free tier: 100 requests/day, 10 requests/minute
API_FOOTBALL_KEY=

# ------------------------------------------------------------------------------
# SUPABASE (REQUIRED)
# ------------------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api
# Used for database, authentication, and real-time features

# Public URL - Safe to expose in client-side code
NEXT_PUBLIC_SUPABASE_URL=

# Anonymous key - Safe to expose in client-side code
# Used for client-side database queries and auth
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key - NEVER expose in client-side code
# Used for server-side operations that bypass RLS
# Keep this secret! Only use in API routes and server components
SUPABASE_SERVICE_ROLE_KEY=

# ------------------------------------------------------------------------------
# STRIPE (REQUIRED FOR GROUP COMPETITIONS)
# ------------------------------------------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys
# Used for payment processing in group competitions

# Publishable key - Safe to expose in client-side code
# Format: pk_test_... (test mode) or pk_live_... (production)
STRIPE_PUBLIC_KEY=

# Secret key - NEVER expose in client-side code
# Format: sk_test_... (test mode) or sk_live_... (production)
STRIPE_SECRET_KEY=

# Webhook signing secret - Used to verify webhook authenticity
# Get from: https://dashboard.stripe.com/webhooks
# Format: whsec_...
STRIPE_WEBHOOK_SECRET=

# ------------------------------------------------------------------------------
# APP CONFIGURATION (REQUIRED)
# ------------------------------------------------------------------------------
# Base URL of your application
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Application display name
NEXT_PUBLIC_APP_NAME=Fixture App

# Node environment (auto-set by Next.js, but can override)
# Options: development, production, test
NODE_ENV=development

# ------------------------------------------------------------------------------
# ERROR MONITORING (OPTIONAL)
# ------------------------------------------------------------------------------
# Sentry for error tracking and monitoring
# Get these from: https://sentry.io/settings/projects/

# Sentry DSN - Safe to expose in client-side code
# Format: https://[key]@[organization].ingest.sentry.io/[project-id]
SENTRY_DSN=

# Sentry auth token - Used for uploading source maps
# Only needed for production deployments
SENTRY_AUTH_TOKEN=

# ------------------------------------------------------------------------------
# VERCEL (AUTO-POPULATED IN VERCEL DEPLOYMENTS)
# ------------------------------------------------------------------------------
# These are automatically set by Vercel - no need to manually configure
# Can be used for environment-specific logic

# Deployment URL (e.g., your-app-abc123.vercel.app)
VERCEL_URL=

# Environment type (e.g., production, preview, development)
VERCEL_ENV=

# Git commit SHA
VERCEL_GIT_COMMIT_SHA=

# ------------------------------------------------------------------------------
# BACKGROUND JOBS & CRON (OPTIONAL)
# ------------------------------------------------------------------------------
# Secret key for authenticating cron job requests
# Generate a random string: openssl rand -base64 32
# Used in API routes that handle scheduled tasks
CRON_SECRET=

# ------------------------------------------------------------------------------
# FEATURE FLAGS (OPTIONAL)
# ------------------------------------------------------------------------------
# Enable/disable features during development
# Set to "true" or "false"

# Enable group competitions feature
NEXT_PUBLIC_FEATURE_GROUPS=true

# Enable notifications
NEXT_PUBLIC_FEATURE_NOTIFICATIONS=true

# Enable multi-sport support (future)
NEXT_PUBLIC_FEATURE_MULTI_SPORT=false

# ------------------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING (OPTIONAL)
# ------------------------------------------------------------------------------
# Enable additional logging in development
# Set to "true" to see verbose logs
NEXT_PUBLIC_DEBUG=false

# Mock API responses (for development without API limits)
# Set to "true" to use mock data instead of real API calls
NEXT_PUBLIC_USE_MOCK_DATA=false
